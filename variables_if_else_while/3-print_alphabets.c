#include <stdio.h>

/**
 * main - Print the alphabet in lowercase
 * Return: 0
 */
int main(void)
{
    char xd;

    for (xd = 'a'; xd <= 'z'; xd++)
    {
        putchar(xd);
    }
    putchar('\n');
    return (0);
}
