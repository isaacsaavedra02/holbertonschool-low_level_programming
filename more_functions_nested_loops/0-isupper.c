#include "main.h"

/**
 *_isupper- función que indica si un carácter o cadena de caracteres es una letra mayúscula
 *@c: variable que da mayusculas
 *Return: 1 si es mayuscula 0 si no
 */
int main() {
int _isupper(int c)
{

if (c >= 'A' && c <= 'Z')

	return (1);
	else
	return (0);
}
return 0;
}
